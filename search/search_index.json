{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"OmniDevX","text":"<p>Batteries-included distribution of OmniDevX \u2014 the PlexusOne developer-experience telemetry domain. One import path composes the canonical event model from <code>omnidevx-core</code> with every available collector.</p> <p>Not OmniDXI</p> <p><code>omnidxi</code> is Digital Experience Intelligence \u2014 product-analytics platforms (Amplitude, Mixpanel, Heap, Pendo). OmniDevX observes how developers and AI agents build software.</p>"},{"location":"#overview","title":"Overview","text":"<p>Composition is by constructor injection through an <code>Engine</code> \u2014 there is no global registry. Every canonical type, event/mode/severity constant, and provider constructor from <code>omnidevx-core</code> and the thick provider modules is re-exported under this one import path.</p> <p>The default bundle is intended for local AI-assistant usage collection. Claude Code, Codex CLI, and Kiro CLI are constructed with their default local-store locations. Git and GitHub are still explicit because they need a repository scan scope or API credentials.</p>"},{"location":"#quick-start","title":"Quick Start","text":"Default (Claude Code + Codex + Kiro)Explicit composition <pre><code>import \"github.com/plexusone/omnidevx\"\n\nengine, err := omnidevx.NewDefault() // local Claude Code + Codex + Kiro stores\nif err != nil {\n    // handle\n}\nresults, err := engine.Collect(ctx, omnidevx.CollectRequest{\n    Period:  omnidevx.Period{Start: weekStart, End: weekEnd},\n    Subject: omnidevx.SubjectRef{PersonID: \"person:jane\"},\n})\n// One collector failing does not discard the others' results: inspect both.\nevents := omnidevx.Events(results)\n</code></pre> <pre><code>claude, _ := omnidevx.NewClaudeCodeCollector(omnidevx.ClaudeCodeOptions{})\ncodex, _ := omnidevx.NewCodexCollector(omnidevx.CodexConfig{})\nkiro, _ := omnidevx.NewKiroCollector(omnidevx.KiroConfig{})\ngit, _ := omnidevx.NewGitCollector(omnidevx.GitOptions{Roots: []string{\"~/go/src\"}})\ngh, _ := omnidevx.NewGitHubCollector(omnidevx.GitHubConfig{\n    Token:    os.Getenv(\"GITHUB_TOKEN\"),\n    Username: \"octocat\",\n})\nengine := omnidevx.New(claude, codex, kiro, git, gh)\n</code></pre> <p>See Usage Guide for composition patterns and Collectors for the full list and what each one needs.</p>"},{"location":"#privacy","title":"Privacy","text":"<p>Events carry metadata only \u2014 never prompt text, model responses, or file contents. See omnidevx-core for the canonical contract.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>go get github.com/plexusone/omnidevx\n</code></pre>"},{"location":"#requirements","title":"Requirements","text":"<ul> <li>Go 1.26 or later</li> </ul>"},{"location":"#related-projects","title":"Related Projects","text":"<ul> <li>omnidevx-core - Canonical event model and contracts</li> <li>omni-github - GitHub DevX collector</li> <li>omni-openai - Codex CLI collector</li> <li>omni-aws - Kiro CLI collector</li> <li>omnidxi - Digital Experience Intelligence (not OmniDevX)</li> </ul>"},{"location":"collectors/","title":"Collectors","text":"<p><code>omnidevx</code> re-exports a constructor for every collector available across the ecosystem. <code>NewDefault</code> composes the local AI-assistant collectors that can use default on-disk stores; the rest need explicit setup and are added with <code>Engine.Add</code>.</p> Source Package Location Setup Claude Code <code>providers/claudecode</code> omnidevx-core (thin, stdlib) none \u2014 reads local session store Codex CLI <code>omni-openai/omnidevx</code> omni-openai (thick, SQLite) none \u2014 reads local <code>~/.codex</code> store Kiro CLI <code>omni-aws/omnidevx</code> omni-aws (thick, SQLite) none \u2014 reads local Kiro CLI database and optional <code>~/.kiro_sessions</code> archives Git <code>providers/git</code> omnidevx-core (thin, stdlib via <code>gogit</code>) <code>Roots []string</code> \u2014 repository roots to scan GitHub <code>omni-github/omnidevx</code> omni-github (thick, REST + GraphQL) <code>Token</code>, <code>Username</code>"},{"location":"collectors/#claude-code-codex-cli-and-kiro-cli","title":"Claude Code, Codex CLI, and Kiro CLI","text":"<p>Included in <code>NewDefault()</code>:</p> <pre><code>engine, err := omnidevx.NewDefault()\n</code></pre> <p>Or individually:</p> <pre><code>claude, err := omnidevx.NewClaudeCodeCollector(omnidevx.ClaudeCodeOptions{})\ncodex, err := omnidevx.NewCodexCollector(omnidevx.CodexConfig{})\nkiro, err := omnidevx.NewKiroCollector(omnidevx.KiroConfig{})\n</code></pre> <p>Kiro local records do not always expose exact token accounting. The Kiro collector emits historical-estimate usage events with reduced confidence when it must reconstruct tokens from local conversation history.</p> <p>The provider-specific packages remain the source of truth for local schema handling and path drift. <code>omnidevx</code> verifies that each provider satisfies the shared collector contract and gives applications one import path for the combined bundle.</p>"},{"location":"collectors/#git","title":"Git","text":"<p>Emits <code>devx.change.committed</code> events, one per commit, with AI co-author attribution ported from the canonical <code>KnownAITools</code> registry. Needs explicit repository roots \u2014 there is no default scan scope:</p> <pre><code>git, err := omnidevx.NewGitCollector(omnidevx.GitOptions{\n    Roots: []string{\"~/go/src\"},\n})\n</code></pre>"},{"location":"collectors/#github","title":"GitHub","text":"<p>Emits <code>devx.profile.snapshot</code>, <code>devx.contribution.snapshot</code> (per repository), and <code>devx.contribution.recorded</code> (daily calendar) events via GitHub's REST and GraphQL APIs:</p> <pre><code>gh, err := omnidevx.NewGitHubCollector(omnidevx.GitHubConfig{\n    Token:    os.Getenv(\"GITHUB_TOKEN\"),\n    Username: \"octocat\",\n})\n</code></pre> <p>The GitHub collector requires a bounded <code>Period</code> (both <code>Start</code> and <code>End</code> set) \u2014 the contribution query needs an explicit range.</p>"},{"location":"collectors/#composing-all-five","title":"Composing All Five","text":"<pre><code>engine := omnidevx.New(claude, codex, kiro).Add(git, gh)\n\nresults, err := engine.Collect(ctx, omnidevx.CollectRequest{\n    Period:  omnidevx.Period{Start: weekStart, End: weekEnd},\n    Subject: omnidevx.SubjectRef{PersonID: \"person:jane\"},\n})\n</code></pre> <p>One collector failing does not stop the others: <code>Collect</code> always returns whatever results succeeded, alongside a joined error naming each failing source.</p>"},{"location":"guides/","title":"Usage Guide","text":"<p><code>omnidevx</code> is the batteries-included package for applications that want a single import path for developer and AI-assistant telemetry collectors.</p>"},{"location":"guides/#choosing-the-bundle","title":"Choosing the Bundle","text":"<p>Use <code>NewDefault</code> when the application should read local AI-assistant stores from the current machine:</p> <pre><code>engine, err := omnidevx.NewDefault()\n</code></pre> <p>The default bundle includes:</p> Collector Source Notes Claude Code <code>omnidevx-core/providers/claudecode</code> Reads Claude Code local session data Codex CLI <code>omni-openai/omnidevx</code> Reads local Codex SQLite and rollout data Kiro CLI <code>omni-aws/omnidevx</code> Reads local Kiro conversation data and may estimate usage <p>Use explicit composition when the caller needs provider-specific configuration or wants to add Git and GitHub:</p> <pre><code>claude, _ := omnidevx.NewClaudeCodeCollector(omnidevx.ClaudeCodeOptions{})\ncodex, _ := omnidevx.NewCodexCollector(omnidevx.CodexConfig{})\nkiro, _ := omnidevx.NewKiroCollector(omnidevx.KiroConfig{})\ngit, _ := omnidevx.NewGitCollector(omnidevx.GitOptions{Roots: []string{\"~/go/src\"}})\ngh, _ := omnidevx.NewGitHubCollector(omnidevx.GitHubConfig{\n    Token:    os.Getenv(\"GITHUB_TOKEN\"),\n    Username: \"octocat\",\n})\n\nengine := omnidevx.New(claude, codex, kiro).Add(git, gh)\n</code></pre>"},{"location":"guides/#handling-partial-results","title":"Handling Partial Results","text":"<p>Collectors are independent. <code>Engine.Collect</code> returns successful results even when one provider fails, and joins provider errors so callers can decide whether partial data is acceptable:</p> <pre><code>results, err := engine.Collect(ctx, req)\nevents := omnidevx.Events(results)\nif err != nil {\n    // Log or surface the provider error without discarding successful events.\n}\n</code></pre>"},{"location":"guides/#provider-boundaries","title":"Provider Boundaries","text":"<p>Thin collectors with minimal dependencies live in <code>github.com/plexusone/omnidevx-core</code>. Thick collectors live in provider modules that already own heavier SDK, API, or storage dependencies:</p> Provider Module Collector <code>github.com/plexusone/omni-openai</code> Codex CLI <code>github.com/plexusone/omni-aws</code> Kiro CLI <code>github.com/plexusone/omni-github</code> GitHub <p>This package imports those provider modules and re-exports their public collector constructors so consumers can depend on <code>omnidevx</code> directly.</p>"},{"location":"releases/v0.1.0/","title":"omnidevx v0.1.0 Release Notes","text":"<p>Release Date: 2026-07-19</p>"},{"location":"releases/v0.1.0/#overview","title":"Overview","text":"<p>Initial release of omnidevx: the batteries-included distribution of the OmniDevX developer-experience telemetry domain. One import path composes the canonical event model from <code>omnidevx-core</code> with every available collector, so consumers depend on a single package instead of wiring <code>omnidevx-core</code>, <code>omni-github</code>, and <code>omni-openai</code> together themselves.</p> <p>Not to be confused with omnidxi (Digital Experience Intelligence / product analytics): OmniDevX observes how developers and AI agents build software.</p>"},{"location":"releases/v0.1.0/#highlights","title":"Highlights","text":"<ul> <li>Re-exported canonical types \u2014 <code>Event</code>, <code>CollectRequest</code>,   <code>CollectionResult</code>, <code>Period</code>, <code>Provenance</code>, <code>Source</code>, <code>SubjectRef</code>,   event-type and collection-mode constants, all under <code>omnidevx</code> so callers   need one import path.</li> <li><code>Engine</code> \u2014 runs an arbitrary set of <code>Collector</code>s over one request.   One collector failing does not stop the others: successful results are   always returned alongside a joined error naming each failing source.   <code>Events</code> flattens results from multiple collectors into one slice.</li> <li>Four collectors, one constructor each:<ul> <li><code>NewClaudeCodeCollector</code> \u2014 Claude Code session history (thin, stdlib,   from omnidevx-core)</li> <li><code>NewCodexCollector</code> \u2014 Codex CLI SQLite + rollout JSONL (thick, from   omni-openai)</li> <li><code>NewGitCollector</code> \u2014 commit history via <code>gogit</code> (thin, stdlib, from   omnidevx-core)</li> <li><code>NewGitHubCollector</code> \u2014 GitHub contribution data via REST + GraphQL   (thick, from omni-github)</li> </ul> </li> <li><code>NewDefault</code> \u2014 composes the local Claude Code and Codex collectors   against the current machine's default stores; Git and GitHub need   explicit configuration (repo roots, tokens) and are added with   <code>Engine.Add</code>.</li> </ul>"},{"location":"releases/v0.1.0/#installation","title":"Installation","text":"<pre><code>go get github.com/plexusone/omnidevx@v0.1.0\n</code></pre> <p>Requires Go 1.26+.</p>"},{"location":"releases/v0.1.0/#usage","title":"Usage","text":"<pre><code>engine, err := omnidevx.NewDefault() // local Claude Code + Codex stores\nif err != nil {\n    // handle\n}\nresults, err := engine.Collect(ctx, omnidevx.CollectRequest{\n    Period:  omnidevx.Period{Start: weekStart, End: weekEnd},\n    Subject: omnidevx.SubjectRef{PersonID: \"person:jane\"},\n})\nevents := omnidevx.Events(results)\n</code></pre> <p>See Collectors for adding the Git and GitHub collectors.</p>"},{"location":"releases/v0.1.0/#package-structure","title":"Package Structure","text":"<pre><code>omnidevx/\n\u251c\u2500\u2500 doc.go\n\u251c\u2500\u2500 omnidevx.go    # re-exported types, constants, constructors, NewDefault\n\u2514\u2500\u2500 engine.go       # Engine: composes and runs collectors\n</code></pre>"},{"location":"releases/v0.1.0/#dependencies","title":"Dependencies","text":"<ul> <li><code>github.com/plexusone/omnidevx-core</code> v0.1.0 \u2014 canonical event model</li> <li><code>github.com/plexusone/omni-github</code> v0.5.0 \u2014 GitHub DevX collector</li> <li><code>github.com/plexusone/omni-openai</code> v0.6.0 \u2014 Codex CLI collector</li> </ul>"},{"location":"releases/v0.1.0/#privacy","title":"Privacy","text":"<p>Events carry metadata only \u2014 never prompt text, model responses, or file contents. See omnidevx-core for the canonical contract.</p>"},{"location":"releases/v0.1.0/#contributors","title":"Contributors","text":"<ul> <li>John Wang</li> <li>Claude Sonnet 5</li> </ul>"},{"location":"releases/v0.1.0/#links","title":"Links","text":"<ul> <li>GitHub Repository</li> <li>Documentation</li> <li>Go Package</li> </ul>"},{"location":"releases/v0.2.0/","title":"omnidevx v0.2.0 Release Notes","text":"<p>Release Date: 2026-07-27</p>"},{"location":"releases/v0.2.0/#overview","title":"Overview","text":"<p><code>omnidevx</code> v0.2.0 adds AWS Kiro CLI support to the batteries-included distribution. The top-level package now composes local Claude Code, Codex CLI, and Kiro CLI collectors behind the same <code>omnidevx-core</code> collector contract, while Git and GitHub remain opt-in collectors because they need explicit repository roots or API credentials.</p> <p>This release keeps the package role unchanged: <code>omnidevx</code> is the convenient single import path for applications that want the full DevX collector set without manually wiring <code>omnidevx-core</code>, <code>omni-openai</code>, <code>omni-aws</code>, and <code>omni-github</code>.</p>"},{"location":"releases/v0.2.0/#highlights","title":"Highlights","text":"<ul> <li>Kiro CLI collector included \u2014 <code>github.com/plexusone/omni-aws</code>   v0.10.0 is now part of the default dependency set.</li> <li>New top-level APIs \u2014 <code>NewKiroCollector</code> and <code>KiroConfig</code> are   re-exported from <code>github.com/plexusone/omni-aws/omnidevx</code>.</li> <li>Expanded <code>NewDefault</code> \u2014 default construction now creates Claude Code,   Codex CLI, and Kiro CLI collectors for local AI-assistant usage telemetry.</li> <li>Provider contract coverage \u2014 tests verify that Codex CLI and Kiro CLI   tolerate empty local stores, while collector errors from unavailable   sources are still returned with successful results from other providers.</li> </ul>"},{"location":"releases/v0.2.0/#installation","title":"Installation","text":"<pre><code>go get github.com/plexusone/omnidevx@v0.2.0\n</code></pre> <p>Requires Go 1.26+.</p>"},{"location":"releases/v0.2.0/#usage","title":"Usage","text":"<pre><code>engine, err := omnidevx.NewDefault() // Claude Code + Codex CLI + Kiro CLI\nif err != nil {\n    // handle constructor errors\n}\n\nresults, err := engine.Collect(ctx, omnidevx.CollectRequest{\n    Period:  omnidevx.Period{Start: weekStart, End: weekEnd},\n    Subject: omnidevx.SubjectRef{PersonID: \"person:jane\"},\n})\nevents := omnidevx.Events(results)\n</code></pre> <p>For explicit composition:</p> <pre><code>claude, _ := omnidevx.NewClaudeCodeCollector(omnidevx.ClaudeCodeOptions{})\ncodex, _ := omnidevx.NewCodexCollector(omnidevx.CodexConfig{})\nkiro, _ := omnidevx.NewKiroCollector(omnidevx.KiroConfig{})\nengine := omnidevx.New(claude, codex, kiro)\n</code></pre>"},{"location":"releases/v0.2.0/#kiro-token-accounting","title":"Kiro Token Accounting","text":"<p>Kiro local records may not expose exact token accounting for every conversation. The Kiro collector therefore emits historical-estimate usage events with reduced confidence when it reconstructs usage from local conversation history.</p>"},{"location":"releases/v0.2.0/#dependencies","title":"Dependencies","text":"<ul> <li><code>github.com/plexusone/omnidevx-core</code> v0.3.0 \u2014 canonical event model</li> <li><code>github.com/plexusone/omni-aws</code> v0.10.0 \u2014 Kiro CLI collector</li> <li><code>github.com/plexusone/omni-github</code> v0.5.0 \u2014 GitHub DevX collector</li> <li><code>github.com/plexusone/omni-openai</code> v0.6.0 \u2014 Codex CLI collector</li> </ul>"},{"location":"releases/v0.2.0/#contributors","title":"Contributors","text":"<ul> <li>John Wang</li> <li>OpenAI Codex</li> <li>dependabot[bot]</li> </ul>"},{"location":"releases/v0.2.0/#links","title":"Links","text":"<ul> <li>GitHub Repository</li> <li>Documentation</li> <li>Go Package</li> </ul>"}]}